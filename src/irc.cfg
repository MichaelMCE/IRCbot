server=irc.quakenet.org
port=6668

nick=Matsuura
altnick=_Aya_

channel=#g15forums.com	; join this channel on startup (channel:password)
channel=		; and this one
channel=		; etc..
channel=
channel=

joins=show		; show join messages in channel (show/hide)
quits=show		; show quit messages (show/hide)
kicks=show		; show kick messages (show/hide)
notices=show		; show notices in channel (show/hide)
invite=yes		; show invite requests (yes/no)
autojoininvite=no	; auto join channel upon invitation (yes/no)
mode=show		; show mode change messages (show/hide)
nickchange=show		; show nick changes messages (show/hide)
ctcps=show		; show CTCP events (displayed in current channel) (show/hide)
query=yes		; allow private messages (yes/no)
querywin=no		; if allowed show private messages in seperate channel, per sender (yes/no)
skipmotd=no		; show message of the day in status screen (yes/no)
autorejoin=yes		; rejoin channel on kick (yes/no)
wordwrap=no		; use word wrapping (yes/no)
highlight=		; TODO: highlight this word
highlight=		; TODO: etc..

botOwnerNick=you nick
botOwnerAddr=your ip
botOwnerIP=82.16.118.127
botPassword=testing!

